/* Rumus untuk berbagai jenis operation, mulai dari yang sendiri, yang binary
hingga yang ternatoryy, dan saya akan mencatat yang ternatory*/
const isMember = false;
const discount = isMember ? 0.1 : 0;
console.log(`anda dapat diskon sebesar ${discount * 100}%`);

//jangan lupa tentang peraturan untuk menggunakan berbagai simbol yang
//dibutuhkan
// lalu hati-hati dengan penggunakan simbol persen dibelakang seratus
// bisa menyebabkan kesalahan pada ouput
